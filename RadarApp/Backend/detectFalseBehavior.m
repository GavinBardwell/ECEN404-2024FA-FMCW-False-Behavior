function isFakeBehavior = detectFalseBehavior(response, rng_grid, dop_grid)
    %INFO
        %response -- post processed FFT informaiton. Is a complex MxP
        %matrix. M is the range measurements and P is the doppler
        %measurements
        %rng_grid -- range samples that FFT was evaluated... of length M
        %dop_grid -- velocity samples that FFT was evaluated... of length P
    %USER CODE GOES HERE
    isFakeBehavior = false();
    %%USER CODE GOES HERE
end

